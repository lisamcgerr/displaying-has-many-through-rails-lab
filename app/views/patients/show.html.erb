<h1><%= @patient.name %></h1>
<% @patient.appointments.each do |appointment| %></h3>
    <p>Appointment Time and Date: <%= appointment.appointment_datetime.strftime("%B %e, %Y at %H:%M") %></p>
    <p>Doctor: <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %></p>
    <% end %>